apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: librespeed-ingress
  namespace: librespeed
  labels:
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
    - web
  routes:
    - match: Host(`librespeed.{{ .Values.cloudflare_zone }}`)
      kind: Rule
      services:
        - name: librespeed
          port: 80
  tls:
    domains:
    - main: {{ .Values.cloudflare_zone }}
      sans:
      - librespeed.{{ .Values.cloudflare_zone }}
    secretName: {{ .Values.cloudflare_zone_hyphen }}-tls
