defaultNamespace: meeting-bot

labels:
  app: meeting-bot-minio

helm:
  # No releaseName on purpose: this bundle started life as Kustomize, so its
  # PVC/Deployment/Service are already owned by Fleet's DEFAULT release name.
  # Setting a custom releaseName here makes Helm try a fresh install and fail to
  # adopt them ("cannot be imported into the current release"). Keeping the
  # default turns it into an upgrade that adopts them cleanly.

  # The directory of the chart in the repo.  Also any valid go-getter supported
  # URL can be used there is specify where to download the chart from.
  # If repo below is set this value if the chart name in the repo
  chart: ""

  # An https to a valid Helm repository to download the chart from
  repo: ""

  # Used if repo is set to look up the version of the chart
  version: ""

  # Force recreate resource that can not be updated
  force: false

  # How long for helm to wait for the release to be active. If the value
  # is less that or equal to zero, we will not wait in Helm
  timeoutSeconds: 0

  # cloudflare_zone / cloudflare_zone_hyphen for the internal-only console IngressRoute.
  valuesFrom:
  - secretKeyRef:
      name: helper-values-secret
      namespace: traefik
      key: values.yaml

# Optional: Configuration if you need to apply specific labels or annotations to the namespace
namespaceLabels:
  managed-by: Fleet
namespaceAnnotations:
  fleet.cattle.io/managed: "true"

# Optional: You might want to pause the rollout to manually inspect before deployment
paused: false

# Deployment strategy can be configured if needed (for larger or more complex deployments)
rolloutStrategy:
  maxUnavailable: 10%

# MinIO needs the S3 credentials secret present first.
dependsOn:
  - selector:
      matchLabels:
        app: meeting-bot-secrets
