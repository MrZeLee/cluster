apiVersion: v1
kind: Secret
metadata:
  name: headscale-router-secret
  namespace: headscale-router
stringData:
  authkey: "HEADSCALE_PREAUTHKEY"
  extra_args: "--login-server=https://HEADSCALE_DOMAIN"