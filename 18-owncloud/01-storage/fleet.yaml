defaultNamespace: owncloud

labels:
  app: ocis-storage

kustomize:
  # StorageClass + data PVC for OCIS. Kept as its own bundle so the app bundle
  # (02-ocis) can depend on it.
  dir: storage

# Optional: Configuration if you need to apply specific labels or annotations to the namespace
namespaceLabels:
  managed-by: Fleet
namespaceAnnotations:
  fleet.cattle.io/managed: "true"

# Optional: You might want to pause the rollout to manually inspect before deployment
paused: false