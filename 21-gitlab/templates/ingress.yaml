apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitlab-ingress
  namespace: gitlab
  labels:
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
    - web
  routes:
    - match: Host(`gitlab.{{ .Values.cloudflare_zone }}`)
      kind: Rule
      services:
        - name: gitlab
          port: 80
  tls:
    domains:
    - main: {{ .Values.cloudflare_zone }}
      sans:
      - gitlab.{{ .Values.cloudflare_zone }}
    secretName: {{ .Values.cloudflare_zone_hyphen }}-tls