apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-config
  namespace: gitlab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # /etc/gitlab — gitlab.rb, secrets, and the persisted SSH host keys.
      storage: 1Gi