apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-data
  namespace: gitlab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # /var/opt/gitlab — git repositories, the bundled Postgres/Redis, uploads.
      storage: 50Gi