defaultNamespace: house-dns

labels:
  app: tailscale-dns

kustomize:
  dir: ""

namespaceLabels:
  managed-by: Fleet
namespaceAnnotations:
  fleet.cattle.io/managed: "true"

paused: false

dependsOn:
  - selector:
      matchLabels:
        app: tailscale-operator