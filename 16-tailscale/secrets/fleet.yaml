defaultNamespace: tailscale
namespace: tailscale

labels:
  app: tailscale-secrets

kustomize:
  dir: ""

namespaceLabels:
  managed-by: Fleet
namespaceAnnotations:
  fleet.cattle.io/managed: "true"

paused: false

# kustomization.yaml + seal-secret-values-secret.yaml in this directory are
# generated by ../../02-secrets/install.sh from the bundle — see 16-tailscale/README.md.