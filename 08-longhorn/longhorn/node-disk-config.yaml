---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: longhorn-disk-configurator
  namespace: longhorn-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: longhorn-disk-configurator
rules:
- apiGroups: ["longhorn.io"]
  resources: ["nodes"]
  verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: longhorn-disk-configurator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: longhorn-disk-configurator
subjects:
- kind: ServiceAccount
  name: longhorn-disk-configurator
  namespace: longhorn-system
---
apiVersion: batch/v1
kind: Job
metadata:
  name: longhorn-disk-config-n5pro-ext4
  namespace: longhorn-system
spec:
  ttlSecondsAfterFinished: 300
  template:
    spec:
      serviceAccountName: longhorn-disk-configurator
      restartPolicy: OnFailure
      containers:
      - name: configure
        image: bitnami/kubectl:latest
        command:
        - /bin/sh
        - -c
        - |
          kubectl patch nodes.longhorn.io n5pro -n longhorn-system --type=merge -p '{
            "spec": {
              "disks": {
                "longhorn-hdd": null,
                "longhorn-hdd-ext4": {
                  "allowScheduling": true,
                  "diskDriver": "",
                  "diskType": "filesystem",
                  "evictionRequested": false,
                  "path": "/mnt/longhorn-hdd-ext4",
                  "storageReserved": 0,
                  "tags": ["hdd"]
                }
              }
            }
          }'
